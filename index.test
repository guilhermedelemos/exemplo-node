const test = require('tape');
const App = require('./index');

test('Mensagem de boas vindas', (t) => {
    const app = new App();
    t.assert(app.saudacao() === "Olá Mundo!", "Mensagem correta");
    t.end();
});
